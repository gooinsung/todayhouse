server:
  port: 8080


spring:
  application:
    name: demo

  datasource:
    platform: mysql
    url: jdbc:mysql://database-1.ckevuiz8guuf.ap-northeast-2.rds.amazonaws.com/todayhouse?autoReconnect=true
    username: ?
    password: ?
    driver-class-name: com.mysql.cj.jdbc.Driver


  servlet:
    multipart:
      max-file-size: 128MB
      max-request-size: 128MB



cloud:
  aws:
    s3:
      bucket: bali-todayhouse
    credentials:
      access-key: ?
      secret-key: ?
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false
